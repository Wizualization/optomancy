// actionTypes.ts
enum ActionTypes {
    // Socket.io actions
    IO_CONNECTED = 'IO_CONNECTED', // A socket.io connection is established
    WORKSPACE_UPDATE = 'WORKSPACE_UPDATE', // Workspace is updated in VSCode
  }
  
  export default ActionTypes;